<%= simple_form_for(@card) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :status %>
    <%= f.input :list %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
